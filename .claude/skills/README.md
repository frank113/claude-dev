# Skills

Each skill lives in its own folder with `SKILL.md` as the entrypoint.
The folder name is the invocation name (e.g. `start-repo/` → `/start-repo`).

See `.claude/README.md` for the full four-tier skills architecture and decision tree.
