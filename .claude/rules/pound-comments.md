# Pound Comments Rules

When writing code in a language that uses a single pound sign (`#`) to denote a comment always use two pound signs (`##`) instead.

An example of a good comment:

```python
## Good comment
```
